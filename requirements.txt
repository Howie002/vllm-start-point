# vLLM Inference Stack — Python dependencies
#
# Install via:  pip install -r requirements.txt
# Or let setup.sh handle it automatically.
#
# vLLM — bundles its own CUDA 12.x libraries; no separate CUDA toolkit needed.
# Blackwell (RTX PRO 6000) requires vllm >= 0.8.0
vllm>=0.8.0

# LiteLLM proxy — [proxy] extra pulls in apscheduler, uvicorn, fastapi, etc.
litellm[proxy]>=1.83.0

# HuggingFace tooling — model downloads, auth
huggingface_hub>=0.23.0

# DuckDB — analytics engine that reads JSONL metrics files directly.
# Used by the agent to aggregate per-minute samples into hourly (or any
# resolution) time-series for the dashboard's Analytics tab. No schema
# migrations, no write-path DB, just SQL over append-only files.
duckdb>=1.0.0
